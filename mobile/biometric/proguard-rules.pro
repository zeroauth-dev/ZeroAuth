# Module-local Proguard rules.
#
# Defer to consumer-rules.pro for the public surface; only add here
# rules that the library itself needs when its own tests run R8.
# Currently empty — the JVM unit tests bypass R8 entirely.
